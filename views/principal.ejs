<%- include ('encabezado.ejs') %>

    <!--boton nuevo producto-->
    <div class="container-fluid p-4">
        <div class="mt-5 mb-2">
            <a href="#" class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#ventana_nuevo_producto"> <img
                    src="/imagenes/nuevoBlanco.png"> Nuevo </a>
            <!--<input type="button" value="Nuevo Producto" class="btn btn-primary">-->
        </div>
        <!-- fin de boton nuevo producto-->

        <!--tabla de productos-->
        <div>
            <div class="card">
                <div class="card-body">
                    <table border="1" class="table table-hover">
                        <thead class="table-dark">
                            <tr>
                                <th scope="col">#</th>
                                <th scope="col">Nombre del producto</th>
                                <th scope="col">Marca del producto</th>
                                <th scope="col">Precio</th>
                                <th scope="col">Stock</th>
                                <th scope="col" class="text-center">Acciones</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% let i=1; for (let fila of resultado) { %>
                                <tr class="align-middle">
                                    <th scope="row">
                                        <%= i++ %>
                                    </th>
                                    <td>
                                        <%= fila.nombre %>
                                    </td>
                                    <td>
                                        <%= fila.marca %>
                                    </td>
                                    <td>
                                        <%= fila.precio %>
                                    </td>
                                    <td>
                                        <%= fila.stock %>
                                    </td>
                                    <td class="text-center">
                                        <a href="/edit?id=<%= fila.id %>" class="btn btn-success"> <img
                                                src="imagenes/check.png"></a>
                                        <a href="/eliminar?id=<%= fila.id %>"
                                            onclick="return confirm('¿esta seguro de eliminar?')"
                                            class="btn btn-danger"><img src="imagenes/delete.png"></a>
                                        <!--con signo de interrogacion indicamos que es un valor que selecciona-->
                                    </td>
                                </tr>
                                <% } %>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
        <!--fin de la tabla de productos-->

        <!--ventana de nuevo producto-->
        <form action="/nuevo" method="post">
            <div class="modal fade" id="ventana_nuevo_producto" data-bs-backdrop="static" data-bs-keyboard="false"
                tabindex="-1" aria-labelledby="staticBackdropLabel" aria-hidden="true">
                <div class="modal-dialog">
                    <div class="modal-content">
                        <div class="modal-header">
                            <h1 class="modal-title fs-5" id="staticBackdropLabel">Agregar nuevo producto</h1>
                            <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                        </div>
                        <div class="modal-body">

                            <label>Nombre de producto</label>
                            <input type="text" name="nombre" class="form-control" required minlength="4" maxlength="30"
                                placeholder="Nombre del producto">

                            <label>Marca</label>



                            <select class="form-select" name="marca">
                                <% for (let fila of marcas) {%>
                        
                                    <option value="<%= fila.id %>"><%= fila.marca %></option>

                                <% } %>
                                
                            </select>

                            <label>Precio</label>
                            <input type="number" name="precio" required placeholder="0" min="-1" max="999"
                                class="form-control">

                            <label>Stock</label>
                            <input type="number" name="stock" required placeholder="0" min="-1" class="form-control">

                        </div>
                        <div class="modal-footer">
                            <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancelar</button>
                            <button type="submit" class="btn btn-primary">Agregar</button>
                        </div>
                    </div>
                </div>
            </div>
        </form>
            <!--fin de ventana de nuevo producto-->
    </div>
    <%- include ('pie_pagina.ejs') %>